---
name: debugkit
version: 0.2.0
description: "调试现场的取证与结案纪律。触发：任何 bug 修复（崩溃/报错/行为异常/数据不对）、症状描述（挂了/不对劲/偶发）、修复完成宣告（修好了/应该好了）、bug 复发（又坏了/老毛病）、上线后返修（用户反馈/线上炸了）。约束 AI 修 bug 时先跑后改、证据先行、结论必带实跑证据，跨会话保存调试现场。"
---

# debugkit — 调试取证与结案纪律

> 本文件是路由器。判定动作后**先读 `references/<对应文件>.md` 再执行**；写案卷文件前查 `references/data.md` 的 schema。

**三条铁律（一切动作的底线）：**
1. **跑在报案前**——没跑过最小复现脚本，不许给结论，也不许改代码
2. **一异常一卷宗**——一个独立症状一份案卷；换症状换案卷，不许混办
3. **结论必带证据**——"修好了"必须跟一条实跑输出；无证据的修复宣告一律视为未修

---

## 第零步（所有动作前）：定位项目与案卷目录

```bash
git rev-parse --path-format=absolute --git-common-dir   # 主仓库 .git
```

- 主仓库根 = 该路径上级；案卷目录 = `<主仓库根>/.debugkit/`（隐藏目录，glob 扫描必须开 `hidden: true`）
- **案卷不进 git**：`.git/info/exclude` 必须含 `.debugkit/`（缺失则立即补上；`.debugkit/` 出现在 git status → 立即修复并 `git rm -r --cached .debugkit`）
- 非 git 目录：以 cwd 为项目根，project.json 标 `"git": false`，禁用 commit/差异取证，纯文档办案
- 身份核对：读 `.debugkit/project.json` 的 key；项目 key = `git remote get-url origin` 规范化（去协议前缀、结尾 `.git`、`':' → '/'`），无 remote 用主仓库绝对路径

## 意图自动识别

**CRITICAL — 绝不问用户"你要立案还是结案"。** 先静默盘点，再按语义路由。

**静默盘点**：定位项目 → 跑 `scripts/dbg.mjs ls`。脚本缺失/报错 → 手工扫描 `.debugkit/cases/` 下各 `case.json`。

| 用户话语信号 | 状态信号 | 判定动作 |
| --- | --- | --- |
| 报告异常（"报错了/挂了/不对劲/数据不对"） | 无 open 案卷 | → 立案（open） |
| 报告异常 | 有 open 案卷且症状不同 | → 另立新案（一异常一卷宗） |
| 报告异常 | 有 open 案卷且症状高度相似 | → 提示关联旧案，用户选：并入旧案（复发计数+1）或另立 |
| 又坏了/老毛病/上次那个 bug 又来了 | 有 merged 或 archived 案卷命中 | → 复发：重开旧案（reopened，复发计数+1），**先查为何复发再修** |
| 描述完现场 / 贴了报错日志 | 有 open 案卷 | → 取证（evidence） |
| 修复宣告（"修好了/应该好了/试试看"） | 有 open/verifying 案卷 | → 结案验证（verify）：无实跑证据 → 拒绝记录，先跑 |
| 用户实测确认好了 | verifying 案卷 | → 结案（close），写根因链 |
| 用户实测没好 / 又出现 | verifying 案卷 | → 判性 reopen：修复宣告作废，回到取证 |
| 问进度/有哪些 bug | 任意 | → status（ls 转述，不改状态） |
| `@debugkit` 无附加语义 | 任意 | → status：open 案卷在前，已结在后 |

**外显**：判定后说一句 `识别为〈动作〉——〈依据〉`，供用户随时纠正。

**确认门槛（高影响动作）**：结案归档（close 需用户实测确认）、复发重开（需复述旧案根因）、假设注入 fix 候选（需用户点头才入 hypothesis 表）。低影响动作（立案、记证据、status）直接做、事后报告。

**兜底**：信号冲突或无法判断 → 不猜。呈现案卷盘点结果让用户选。

## 动作路由（判定后先读对应文件再执行）

| 判定动作 | 详情文件（先读再执行） |
| --- | --- |
| 立案 | `references/open.md` |
| 取证与假设 | `references/evidence.md` |
| 修复与回归 | `references/fix.md` |
| 结案与复发 | `references/close.md` |
| 数据结构 / schema | `references/data.md` |

**status（内联）**：跑 `scripts/dbg.mjs ls` 转述输出，不改状态；脚本缺失 → 手工读 case.json 拼同列表（id / 症状一句话 / 状态 / 假设数 / 已排除数 / 复发次数 / 最后活跃）。

**脚本（只读工具）**：`scripts/dbg.mjs`——`ls` 盘点 / `doctor` 体检（不变量 D1-D5，退出码 0=全绿 1=有差异）/ `stats` 结案率与复发率盘点。**零写入**：修复动作由编排层落笔，绕不开确认门槛。

## 硬规则（违反任何一条：立即停止并向用户报告）

1. **结论必须带证据 id**——任何"根因是 X""已修复"的表述，句尾必须挂 `（证据 E<n>）`；无证据 → 重跑取证。
2. **改代码前必须有 open 假设**——没有假设表的修复改动 = 瞎改，立即停止；假设进表须用户确认（高影响门槛）。
3. **一异常一卷宗**——发现症状变化（报错信息变了/挂的位置变了）→ 强制另立新案，禁止在旧案里混修。
4. **复发 = 回归**——重开旧案时**先查为何复发**（回退了？条件漏了？根因就没找对？），禁止直接再补丁；查清前冻结该案所有修复动作。
5. **证据只追加不修改**——写错的证据标记 `superseded`，原条目保留；伪造/脑补证据 = 立即停止并报告。
6. `.debugkit/` 出现在 git status → 立即修复 exclude 并从暂存区移除。
7. **只操作本项目的 `.debugkit/` 与本仓库**；绝不碰其他项目案卷。
8. **验证不可降级**——verify 的复跑必须实跑用户原始症状的等价命令；禁止用"代码看起来对了"替代跑通。

## 模板

`templates/` 下四个模板：`case-report.md`（结案报告）、`findings.md`（排查笔记）、`session-log.md`（会话日志）、`regression-postmortem.md`（复发复盘）。复制后替换 `<占位符>`；无内容的节保留标题并写"（暂无）"，不要删节。
